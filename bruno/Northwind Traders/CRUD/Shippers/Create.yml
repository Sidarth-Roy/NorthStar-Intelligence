info:
  name: Create
  type: http
  seq: 2

http:
  method: post
  url: http://localhost:8081/api/v1/shippers
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "companyName": "Test"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
