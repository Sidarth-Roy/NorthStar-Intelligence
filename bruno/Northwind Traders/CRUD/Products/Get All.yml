info:
  name: Get All
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8081/api/v1/products
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
